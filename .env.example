# Video Downloader Environment Configuration

# For better TikTok and Instagram support, configure browser cookies
# Choose ONE of the following options:

# Option 1: Use browser cookies (recommended)
# Set to your browser name: chrome, firefox, edge, safari
# COOKIES_BROWSER=chrome

# Option 2: Use cookies file (advanced)
# Path to cookies.txt file in Netscape format
# COOKIES_FILE=/path/to/cookies.txt

# Server Configuration
PORT=5000
NODE_ENV=development

# Database (if using external database)
# DATABASE_URL=your_database_url_here
